# UI 范围落地决策日志

## D01 — 采用完整批准范围

用户已确认网页支持及Query/Router纳入本期，采用67 Story/1089 GWT方案。9.6/9.7从候选转为正式独立Story，不合并为UI重构。

## D02 — 更新既有合同与保留停止边界

依据当前CURRENT/Sprint，1.7资源阻断且完成后停止；此次用户要求对应调整。先完整完成已授权规划/实际合同/就绪和既有9.1的平台最低配置同步。新9.3–9.7进入backlog，无自动开发派发；不恢复3.1，不冒充真机/服务完成。

## D03 — 工作流适配与输入

使用Correct Course已批准提案收口，CA续接已有架构决定，PRD/UX Update更新既有权威文档，CE只合入37组已批准增量GWT，IR定向验证。用户已要求连续执行、不重复询问已确定选型；沿用已批准输入而不重新发现产品或重置模板。PRD/UX按技能要求分派限定提取/整理任务；历史文档格式和原型依据保留，不新建竞争权威。

输入已完整读取并冻结于archive/ui-foundation-2026-09-20（62份显式文件，不含env、秘密或构建物）。主任务已通过read_thread核实为idle，真实资源阻断记录保持，不发送重新开始开发的消息。

## D04 — 验证链与历史来源

schema3逐层验证当前UI及先前Capacitor批准、snapshot和旧catalog/delivery，已封存packet内的Source片段作为缺少独立副本的历史源，不能回退未来实时源导致普通维护被阻断。现行镜像仍和当前源逐片一致。所有新增条件/义务进入实际Tasks和逐Story证据。

## D05 — 平台小版本的可重复同步

当前Capacitor8.5.2 sync根据Xcode主版本生成App SPM，故提供限定wrapper保留16.4。所有Xcode普通/条件目标、Web JS/CSS和SPM一起验证，未知平台声明失败。修补自有配置/脚本，不修改node_modules或假报iOS真机。

## D06 — 有界开发依赖与后续恢复

9.4/9.5可独立交付现有组件工作台和既有页面保护；9.3启动需要两者已交付。9.6/9.7在此基础上另消费9.3实际通过的本地回归切片，不需要把9.3全native完成作为普通读取代码前置。当前停止边界不变；未来恢复记录须明确新停止点、允许集合和真实用户原话，检查器限制越界派发。文件只记录授权。

## D07 — 限定CR与工作流收口

三层CR按批准范围直接修复10项，无新决策/延期；原审阅者定向复核关闭。82项交接回归、19+5配置测试、mobile build、native sync/verify通过。CA/CE/IR依据既有批准连续完成定向更新；bmad-help目录确认后续路由CS→VS→DS→CR，但当前1.7停止优先。本轮不把9.1或新5张Story标done。
