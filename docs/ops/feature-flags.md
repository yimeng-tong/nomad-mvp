# Feature Flags (MVP v0.3-light)

- planner_autoplace_v1: enable/disable AI seed placement
- near_hotel_enabled: enable near_hotel scoring boost when hotel selected
- result_sheet_light_edit_enabled: allow slot text overrides on ResultSheet
- search_topk: integer (default 5) for FR44‑lite text search
- hotel_stickiness_enabled: default true; same-city carry-over

# Post‑MVP (planned)
- multi_city_enabled: enable transport_slot segmentation (v1.0)
- hotel_auto_replan_enabled: prompt replan on hotel change (v1.0)
- history_snapshots_enabled: timeline snapshots & rollback (v1.0)
