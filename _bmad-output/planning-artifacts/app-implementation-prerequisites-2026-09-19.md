# App 工程条件（2026-09-19 已批准）

来源：[范围批准](capacitor-scope-decision-2026-09-19.md)。原七项 OPS/DB/DATA/METRICS 条件保留；新增以下条件，逐 Story 消费、逐切片记录，不因 SDK 存在或一张 Story 通过而全部关闭。

## APP-BUILD-01：原生工程与可重复构建安装

首次责任 9.1，9.2 再核验最终候选。开始时记录实际 Node/Capacitor/JDK/Gradle/SDK/Xcode、应用标识/环境和签名资源安全引用；未知明确列出，允许先做不依赖该资源的实现。日常 WSL Node/pnpm，iOS 原生使用明确 macOS 工作区，Windows Node/pnpm 禁令保留。

关闭需要 Web 和双端原生受控构建、实际设备安装、版本/资源摘要及最小权限证据；原生源目录、CI workflow、编译或模拟器不替代真机。候选不含服务端秘密、开发身份或发布 live-reload URL；iOS16 支持同时约束 Web bundle 目标。记录升级兼容和真实失败，不删除已有业务数据。

## APP-HOST-01：逐业务宿主交互与恢复

9.1 建立矩阵，所有适用旅行者 UI/原生消费 Story 完成自己的行；纯桌面或无宿主变化切片给出具体不适用依据。开始条件是当前源 Story、实际宿主/插件/权限和被消费平台接口明确，不能用框架模拟替代真实证明。

关闭按实际切片核验 Android/iOS 返回/键盘/安全区/读屏、权限拒绝/撤回、冷暖启动/挂起/进程重建、owner/session/任务/revision 对账及迟到回调隔离。1.0 负责真实登录/API/SSE/下载及归因，5.5 负责相册/分享真实结果，6.2 负责单次定位/坐标/无GMS降级，7.x负责原生数据/账号/反馈，8.1负责版本化故障与分母。浏览器证据不能单独关闭原生行。

## APP-DISTRIBUTE-01：APK 与 TestFlight 交付

首次责任 9.2。开始时核对本期业务验收、候选清单、Android签名及既有包名、Apple团队/应用/TestFlight实际测试组权限；只记录安全引用，不传播凭据或邀请未指定人员。

关闭需要可安装的签名APK、校验和和说明，指定测试组可用且实际iPhone安装的TestFlight build，以及两端安装/升级、关键业务/失败恢复、版本与有效期证据。上传受理、IPA、开发包与最终TestFlight可用分开。保留可执行的停止分发/替换/API兼容方案，不承诺任意iOS原地降级、热更新或远程擦除外部副本。

## 持续执行与决策记录

常规准备、开发、验证和审阅已经获授权；具体技术选择可先决定并写ADR，记录背景、可选方案、证据、后果和可逆性。资源等待不得伪装为 done：记录阻断的具体能力与可并行任务；共享后端/其他独立 Story 可继续。3.1真实上游和迁移审计保持，不通过授权字段跳过依赖。
