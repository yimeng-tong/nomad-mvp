---
project: nomad-mvp
date: 2026-09-13
workflow: bmad-create-epics-and-stories-step-3
epic: 8
story: '8.6'
status: approved-and-appended
storyApproved: true
visualApproved: true
appendedToEpics: true
researchDelegated: true
researchStatus: complete
researchDecisionApproved: true
approvedDate: 2026-09-13
operatorSurface: desktop-web-only
chatPresentation: chinese-numbered-prose
runtimeValidated: false
---

# Story 8.6 Review: 运营总览与只读排障入口

8.5的24条GWT、采用方向及两张基础R1已获批准并追加。当前继续CE Step3，准备最后
一张MVP运营Story。用户于2026-09-13认可采用方向、20条GWT/中文验收说明及两张R1，
本合同已正式追加并同步。批准不代表已实施、Epic8整体关闭或最终CE/IR/SP通过。
8.7/8.8保留Post-MVP。对话展示遵循用户确认的中文编号说明，正式文件保留BMAD GWT。

## Story

As a Nomad 运营者，
I want 在同一只读总览核对服务质量、耗时、成本、降级和告警，并进入对应工具查看依据，
So that 我能快速判断需要处理的问题，并依据正确的数据范围排障。

**Requirements:** FR13、FR14、FR33、FR38、FR38.1；NFR3-NFR7、NFR17、NFR20、NFR24；
AR1-AR6、AR11-AR15、AR17-AR20、AR22；UX-DR2-UX-DR3、UX-DR31-UX-DR33。

## 调研与已批准采用

- 两组研究分别核对Sentry/Langfuse的原生dashboard/指标API和Grafana/薄Web组合方式，
  均包含官方与固定GitHub证据。汇总见
  [成熟方案研究](research/story-8-6-mature-implementations-2026-09-13.md)。
- **建议采用现有Node/React受限桌面Web上的薄只读总览，复杂分析继续进入原工具。**
  价值是将8.2质量、8.4账本、8.5事件与8.1观测正确并列；不新建通用图表平台或把所有
  事实搬进一家监控产品。本期不要求新增Grafana、iframe、公共snapshot或匿名共享。
- 新页面方案已于2026-09-13获批，尚未实施。实际Sentry/Langfuse/Grafana部署、版本、套餐、
  API权限/配额及历史数据可达范围未核验，不能因官网存在API就认定当前环境可用。
- 本地基线以已批准8.1-8.5合同为目标；当前旧SDK/占位接入不证明目标数据已交付。
  8.6不补造缺失事实，也不把前序Story的运行验证转移到总览后才能完成。

## 数据来源与时间口径

| 模块 | 权威来源及时间 | 核心约束 |
| --- | --- | --- |
| 任务结果/降级 | 既有持久业务任务/事件的同一能力、同一已定义任务群及时间窗口 | 一个逻辑Job不是一个Provider attempt；终态窗口与接收窗口不能偷换 |
| 执行耗时/错误趋势 | 8.1已登记的观测聚合或已有明确业务计时事实 | 记录阶段、成功/失败/排队计时范围、样本、采样/外推方式；不把p95再平均 |
| 质量回归 | 8.2封存的样本、规则、模型/prompt、自动/人工结果快照 | 报告版本和完成度独立，不能当作当前全量线上质量；不恢复统一硬规则否决 |
| 预算/成本 | 8.4账本、策略/资费版本及匹配供应商证据 | 账期/币种/估算/已核对/预留/未知分开，上游余额独立；不由采样trace拼账 |
| 当前告警/投递 | 8.5当前故障周期和投递记录，以来源as-of为准 | 事件与通知状态分开，静默/无数据/已读假设不能推导服务恢复 |
| 配置摘要 | 8.3/8.4/8.5各自只读版本及生效证据 | 发布、加载和实际使用不混同，总览无修改权威 |

每项携带来源/定义版本、操作者可见范围、请求窗口、实际覆盖窗口、源数据截至时间、
本次读取时间和状态。封存报告时间、预算账期、当前事件as-of可以并列，不能强行用一个
“最近24小时”筛选伪装同时快照。返回200或空数组不证明完整历史均已覆盖；保留/套餐
裁剪、分页/Top-N、采样/外推和缺失数据必须明示。不能相加独立工具的trace来估计全量。

服务端只代理少量固定只读查询，保留8.4/8.5原权威，使用有限共享缓存和请求合并以适配
真实API限额。缓存隔离按有效权限/环境/范围而非单纯URL。读取源失败不能改变业务状态；
实际来源不支持某API时保留未接入及原工具入口，不能用模拟数据填补核心交付缺口。

## 面向对话评审的主要验收说明

1. 总览只汇总已交付的质量、耗时、成本、降级和告警，并链接到已有功能；未接入的能力明确标注。
2. 运营者在桌面 Web 查看，服务端按账号和环境权限限制数据；普通旅行用户没有这个入口。
3. 每项数据都登记来源和指标定义，只开放固定的只读查询，不让浏览器提交任意 SQL、地址或查询语句。
4. 切换环境、能力和时间时说明实际筛选范围；质量报告版本、预算账期和当前告警分别标明，不假装共用同一时间窗口。
5. 每个模块展示数据截至时间、覆盖区间和来源状态；查询被截断、超出保留期或只返回部分结果时明确说明。
6. 业务完成率按同一口径的逻辑任务计算，重试不增加任务数；分母为零时显示暂无数据，不显示百分之百成功。
7. 耗时展示对应阶段、有效样本和采样情况，不把多个 P95 取平均，也不把采样或外推结果标成全量实测。
8. 质量摘要来自固定版本的评价报告，保留逐规则结果、人工复核、未完成和执行错误，不用一个平均分替代既有评价政策。
9. 成本直接读取预算账本，分开显示已核对费用、资费估算、预留与未知占用；供应商余额另列，缺少账单来源不记作零。
10. 告警读取事件权威，服务状态与通知状态分开；没有告警、通知发送成功或一段时间没有数据，都不等于服务已恢复。
11. 配置摘要区分已发布、已加载和实际使用的版本，已有任务仍可使用原快照；总览不发布或回滚配置。
12. 单个来源超时、限流、无权限或未接入时，只标明该模块的问题，其余可用数据继续显示；陈旧数值保留时间，不伪装成最新值。
13. 快速切换筛选或并发刷新时，迟到响应不能覆盖新范围；切换账号、退出或权限收紧后清除不可再看的缓存。
14. 刷新采用有界查询、共享请求、缓存和退避，遵守来源 API 的实际额度；缓存命中不能把数据时间改成当前时间。
15. 缓存按实际可见范围隔离，来源权限不足时不能借用服务账号或别人的缓存扩大可见数据。
16. 排障链接只去已登记的受保护页面，并说明带入的环境、时间和筛选；目标工具独立登录鉴权，不用公开分享或 URL 密钥绕过。
17. 桌面页面支持键盘、焦点及清晰的加载、空态和错误提示；改变图表宽度或显示粒度不能改变权威统计数字。
18. 只取必要聚合和安全关联，不为总览拉取用户原文、行程、位置、反馈附件或密钥；评测工作区的内容权限不会扩大到总览。
19. 打开、刷新或关闭总览不启动模型调用、评测、重跑、策略修改或测试消息；需要操作时进入对应功能的原有确认流程。
20. 用真实已接入来源验证口径、部分故障、权限、缓存、筛选和链接，并保留桌面证据；缺失核心数据、模拟结果和原型不能代替实际交付。

## Acceptance Criteria

### 1. 只汇总已经交付的能力

**Given** 前序业务与8.1-8.5已提供各自真实数据/可用入口
**When** 运营者打开总览
**Then** 同页核对已登记质量、耗时、成本、降级、告警和相关配置摘要，再进入各自来源查看依据
**And** 未交付能力不显示为正常/可用，缺失事实不在此重新采集或伪造，不依赖8.7/8.8

### 2. 桌面运营权限

**Given** 请求页面或汇总API
**When** 服务端解析当前会话和可见环境/范围
**Then** 使用真实运营读权限授权，普通旅行用户不能访问，前端隐藏导航不替代后端检查
**And** 仅要求桌面Web，不增加移动运营适配或用户Settings入口，既有用户移动业务合同保持

### 3. 固定来源与查询登记

**Given** 来源具有已验证的版本、只读能力和权限
**When** 注册指标或查询它
**Then** 固定来源host/project、字段、维度、单位、查询版本、最大窗口/行数、权限及数据可访问边界，服务端验证参数
**And** 不开放任意SQL/URL/筛选表达式代理，不把应用DB owner或服务key直接给浏览器；无对应来源能力时返回未接入

### 4. 筛选与独立窗口

**Given** 选择环境、已登记能力、Provider或时间
**When** 请求并展示各摘要
**Then** 每项使用实际支持且获授权的过滤，明示业务任务群/事件窗口、质量报告版本、预算账期与当前故障as-of
**And** 不支持的筛选明确说明/禁止假套用，不把封存报告或实时事件伪装成共用业务时间筛选的结果

### 5. 来源、覆盖与真实新鲜度

**Given** 来源返回数据、部分数据或空集
**When** 构造模块状态
**Then** 保留请求与实际覆盖范围、来源/定义版本、源数据截至及读取时间、样本/采样/外推信息和截断/保留期限制
**And** 成功HTTP不代表全量覆盖，未知覆盖不填100%；缓存读取不更新源数据时间，超过可访问历史不显示为没有业务
**And** 来源未提供的截止时间/覆盖信息明确标未知，不以本次HTTP返回时间推造数据已更新

### 6. 任务计数与比例

**Given** 计算完成、失败或降级计数/比例
**When** 聚合同一能力和版本化任务群
**Then** 按业务逻辑Job身份去重，明确接收/终态窗口、完成/失败/取消/进行中如何计入；重试/子attempt与用户数独立
**And** 比例的分子/分母来自匹配范围，分母为零显示暂无可计算值，不平均不同范围百分比，不相加跨工具独立采样计数

### 7. 耗时与采样结果

**Given** 展示P50/P95或错误趋势
**When** 读取已登记计时聚合
**Then** 明确阶段、排队/执行和成功/失败的包含范围、有效样本、时间与采样/外推方式，按该定义读取可解释结果
**And** 不平均多个P95、不用有限图点重新计算权威百分位，不把采样/粗略外推当全量实测；样本不足显示限制

### 8. 质量报告与人评

**Given** 总览关联8.2评价结果
**When** 展示质量摘要和打开报告
**Then** 固定样本/模型/prompt/规则/代码与评价版本，区分自动结果、人工复核、执行错误、未完成和未运行，链接到对应封存报告/工作区
**And** 保留逐规则计分/提醒/人工复核政策，不恢复统一硬失败否决或仅可选摘要；离线回归分数不标为实时全量质量，浏览不启动新评测

### 9. 预算账本与金额来源

**Given** 展示预算和消耗
**When** 读取8.4已授权范围及账期
**Then** 直接消费其已核对/估算/未发出预留/在途未知、币种/资费来源和可新预留结果，保留原计算与账期，必要时分币种显示
**And** 不拿Langfuse成本或trace计数补账，不把未知/未接入记零，不混同上游余额、Nomad上限和实际扣费，不无依据跨币种相加

### 10. 当前故障与通知状态

**Given** 8.5提供当前事件/投递事实
**When** 展示待处理事件与通知摘要
**Then** 使用对应故障周期和来源版本，分开服务活跃/恢复/陈旧、通知静默、已返回消息记录/未知/拒绝，计数与来源一致
**And** 0个事件不等于全服务健康，通知成功不代表已读/业务恢复，缺数据不生成恢复或新告警

### 11. 配置版本摘要

**Given** 来源提供路由、预算或告警策略的已发布/生效证据
**When** 展示当前配置及查看详情
**Then** 区分发布回执、已加载和实际使用版本，保留旧任务快照及未验证/等待使用状态
**And** 不把新发布等同全实例使用，不在总览重新实现编辑/发布/暂停/回滚或修改任何配置权威

### 12. 各来源独立失败

**Given** 某源超时、429、401/403、未配置或缓存过期
**When** 其他源仍能返回数据
**Then** 分模块显示加载、未接入、空数据、查询失败、权限不足、限速或陈旧状态，保留仍获授权的其他事实与安全重试/入口
**And** 仅在完整有效查询确为空时显示0；陈旧值始终带原时间和状态，不能一处失败让全页清零或显示全局正常

### 13. 筛选切换与迟到结果

**Given** 快速切换环境/时间/能力、重连或并发刷新
**When** 旧请求迟到或账号/权限发生变化
**Then** 用请求范围/代次核验响应，只更新匹配的当前视图；切换后不混入旧环境数据，原筛选上下文可恢复
**And** 登出/换账号/撤权及时清除不可见数据、请求和缓存展示，失败不能重新显示已失去权限的旧快照

### 14. 有界查询和共享刷新

**Given** 多个运营者或多个模块同时请求刷新
**When** 查询PG及外部聚合API
**Then** 使用独立来源超时、有限并发/行数/窗口、同范围请求合并、共享缓存和退避，按实际套餐/响应限额调度；后台/隐藏页面停止不必要轮询
**And** 不用多key绕限，不让每个浏览器重复耗尽组织配额；手动刷新仍说明实际重新查询/缓存命中和可重试时间，不触发AI/AMap业务调用

### 15. 缓存不扩大权限

**Given** 复用聚合缓存或使用服务端来源凭据
**When** 返回给不同操作者
**Then** 缓存键/读取校验包含有效可见范围、环境、过滤、来源与定义版本，权限/来源映射变化使相关缓存失效
**And** 服务凭据能力不等于浏览器用户授权，不能借其他账号缓存或高权来源返回不可见项目；不靠图表/变量/Viewer标签充当数据隔离

### 16. 受保护的排障链接

**Given** 运营者选择原工具、质量报告或既有配置/事件页面
**When** 构造和打开链接
**Then** 使用登记host/project/path及实际支持的安全过滤/绝对时间，说明哪些条件带入，目标独立校验登录和权限，返回可恢复原筛选
**And** 链接不是授权，不携带密钥、免登录token或私人行程；失效/无权不降级成公开分享，目标widget覆盖筛选时不得声称范围一致

### 17. 桌面表现与稳定数字

**Given** 使用键盘、调整桌面窗口或查看有限图表
**When** 页面加载、切换、错误或重新布局
**Then** 提供明确文字状态、焦点/键盘操作、可读表格和非颜色区分，避免遮挡与错误加载完成感
**And** 图宽、最大点数、展示粒度变化只影响绘制，不改变权威成本/计数/P95；无需手机截图或触控适配，复杂图表编辑留在原工具

### 18. 聚合隐私与安全字段

**Given** 从业务/工具来源获取摘要、metadata或链接
**When** 读取、缓存、展示或记录查询日志
**Then** 只选获准聚合与最小安全关联并验证不可信字段，不为计算总览拉取用户原文、精确行程/位置、反馈附件或secrets
**And** 不提供用户内容钻取，不因8.2获准评测副本扩大生产遥测/总览权限；来源清理/撤权后清理相关缓存，不自动开启公共snapshot、匿名iframe或导出分享

### 19. 只读与故障隔离

**Given** 打开、刷新、退出或总览自身不可用
**When** 查询和导航执行
**Then** 不启动模型/评价/探测/测试消息、重跑任务或修改策略/预算/告警；需要操作时进入对应Story现有鉴权与明确确认流程
**And** 查询故障不改变Job/Plan、8.4账本或8.5恢复/投递事实；有界查询不无约束争用业务连接池，不另建客服/值班/观测计量平台

### 20. 最小迁移与真实交付证据

**Given** 本Story准备交付
**When** 在实际已授权来源及桌面环境验证
**Then** 固定并核实来源版本/套餐/API能力/权限/保留与限额，真实覆盖各核心摘要、口径/窗口/金额来源、链接筛选、部分故障、采样/截断、缓存/撤权和迟到响应
**And** OpenAPI与生成类型、最小只读适配/查询及相关测试/构建/桌面证据完成；未接入核心源、模拟数字、SDK存在或原型不算交付，缺失前序事实回到对应Story补证
**And** 不要求新Grafana/iframe/平台升级，未知部署/权限仍列门槛；本Story批准不自动关闭Epic8或跳过整体覆盖、最终CE/IR/SP

## 已批准原型与下一检查点

- [Overview R1](../implementation-artifacts/visual/story-8-6-operations-overview-r1.png)：桌面总览的任务、耗时、评价、账本、事件和配置来源。
- [Source States R1](../implementation-artifacts/visual/story-8-6-overview-source-states-r1.png)：来源超时/陈旧、有效空集与切换环境后的独立权限状态。

全部为合成示例，非生产结果。时间、数量、币种和版本不成为默认阈值；两图未逐一覆盖
长表分页、全部来源无权、缓存撤权竞态、深链登录/保留失效等分支，仍须实施桌面证据。
采用方向、20条GWT/对应中文说明及两张基础R1已批准并追加/同步。当前进行Epic8
覆盖核对及整体确认，见epic-8-coverage-review-2026-09-13.md；不直接运行最终CE/IR/SP
或恢复业务实施，不重新请求8.6批准。

本轮准备核验（2026-09-13）：20条GWT与20条中文编号说明顺序/结构一致；两张1536×1024
图片、生成/修订提示及研究本地链接已核对。8.5的24条GWT逐字追加一次，已批准FR38.1
补充之外的旧epics正文保留；PRD/架构/UX源与镜像一致，handoff与diff检查通过。
业务代码、API/Prisma、部署/工具脚本、实施Story、历史Sprint和已有PNG未变；没有查询
生产数据或执行本Story运行测试，真实来源和桌面验收仍是实施门槛。
