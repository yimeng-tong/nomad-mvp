# Existing Nomad runtime names. Only fill after self-hosted services are ready.
# Merge into the backend environment; do not overwrite unrelated settings.
SENTRY_DSN=
LANGFUSE_HOST=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# Full POST endpoint, not just a host. The upstream response adapter is pending.
# Future target: http://<reachable-homelab-host>:5556/xhs/detail
XHS_DOWNLOADER_URL=

# OSS/CDN and U-Link are deliberately not configured at this stage.
